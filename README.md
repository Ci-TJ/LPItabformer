# LPItabformer

## Installation
```bash
pip install lpitabformer
```

## Example
The example code of LPItabformer is in `example/LPItabformer.zip`, you can download and unzip it and use it according to `README.txt`. `dsuTab_cz.ipynb` is the main example.
